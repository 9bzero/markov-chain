# Contributing

Open an issue before working on anything significant — especially visualization logic, which is easy to get subtly wrong.

Bug fixes can come directly as PRs.

```bash
npm install && npm run dev
```